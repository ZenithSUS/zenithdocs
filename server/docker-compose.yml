services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: zenithdocs-server:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: npm run start

  worker:
    image: zenithdocs-server:latest
    env_file:
      - .env
    command: npm run worker
